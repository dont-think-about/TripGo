name: Android CI

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Cache Gradle packages
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', '**/buildSrc/**/*.kt') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Access Google Service
        run: echo '${{secrets.GOOGLE_SERVICES_JSON}}' > ./app/google-service.json

      - name: Access KAKAO Key
        run: echo KAKAO_API_KEY='${{secrets.KAKAO_API_KEY}}' > ./local.properties

      - name: Access TOUR Key
        run: echo TOUR_API_KEY='${{secrets.TOUR_API_KEY}}' > ./local.properties

      - name: Access SK Key
        env:
          APOD_API_KEY: ${{secrets.SK_OPEN_API_KEY}}
        run: echo SK_OPEN_API_KEY=\"$SK_OPEN_API_KEY\" > ./local.properties

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build

      - name: Check ktlint format
        run: ./gradlew ktlintCheck


